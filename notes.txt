manually setup bridge, disconnect all other adapters first
WSL
nano etc/hostname
    MSI-WSL










# Docket IPvlan L3 from the bridge ##no
docker network create -d ipvlan --subnet 172.20.96.0/20 -o parent=eth0  -o ipvlan_mode=l3 test_ipvlan
--gateway 172.20.96.1
 


# udf bridge ##no has internet though
docker network create new_bridge

# 
docker network create -d macvlan --subnet 172.20.96.0/20 --gateway 172.20.96.1 -o parent=eth0 test 